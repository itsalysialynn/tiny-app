<!DOCTYPE html>
<html lang="en">
<head>
  <%- include partials/_header.ejs %>
</head>
<body>
  <ul>
  <% for(var key in urls) { %>
    <li><%= key + ': ' + urls[key] %></li>
    <% } %>
  </ul>
</body>
</html>